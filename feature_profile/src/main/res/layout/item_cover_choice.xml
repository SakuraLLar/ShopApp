<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_cover_item"
    android:layout_width="120dp"
    android:layout_height="60dp"
    android:layout_margin="8dp"
    android:clickable="true"
    android:contentDescription="@string/select_cover_img"
    android:focusable="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:strokeColor="?attr/colorPrimary"
    app:strokeWidth="0dp">

    <ImageView
        android:id="@+id/img_cover_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/content_description_img"
        android:scaleType="centerCrop"
        tools:src="@drawable/cover_gradient_leaves" />

</com.google.android.material.card.MaterialCardView>
